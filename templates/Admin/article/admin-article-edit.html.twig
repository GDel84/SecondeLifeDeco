{% extends 'admin/base-admin.html.twig' %}


{% block body %}
<div class="carteForm">
    <h1 class="titleForm">Modifier article</h1>
 

    {{ form_start(articles) }}
    {{ form_widget(articles) }}
        <button type="submit" class="btn btn-success">Modifier</button>
    {{ form_end(articles) }}
</div>

{% endblock %} 