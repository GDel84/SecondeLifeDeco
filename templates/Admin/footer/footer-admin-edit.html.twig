{% extends 'admin/adminNav.html.twig' %}

{% block title %}modifier un pied de page{% endblock %}

{% block body %}
<div class="carteForm">
    <h1>Modifier le pied de page</h1>
 

        {{ form_start(footer) }}
        {{ form_widget(footer) }}
            <button type="submit" class="btn btn-secondary">Ajouter</button>
        {{ form_end(footer) }}
</div>
{% endblock %}