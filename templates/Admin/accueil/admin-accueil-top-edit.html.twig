{% extends 'admin/base-admin.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="carteForm">
    <h1 class="titleForm">Modifier accueil</h1>
 

        {{ form_start(accueilTop) }}
        {{ form_widget(accueilTop) }}
        <div class="btnForm">
            <button type="submit" class="btn btn-success">Modifier</button>
        </div>
        {{ form_end(accueilTop) }}
</div>
{% endblock %}