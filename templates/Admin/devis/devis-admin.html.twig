{% extends 'admin/base-admin.html.twig' %}

{% block title %}Les devis{% endblock %}

{% block body %}
<div class="titre-section-info">
    <h1 class="">Les Devis</h1>
    <a href="{{ path("admin-devis-create") }}" type="button" class="btn btn-success">Ajouter</a>
</div>

<section class="sectionDevis">
    {% for devis in deviss %}
    <div class="carteDevis">
        <p class="devisName"><span class="devisp">Nom, Prénom :</span>{{devis.name}} {{devis.lastName}}</p>
        <p><span class="devisp">eamil :</span>{{devis.email}}</p>
        <p><span class="devisp">Date :</span>{{devis.date|date("m/d/Y")}} au {{devis.dateFin|date("m/d/Y")}}</p>
        <p><span class="devisp">Livraison :</span>{{devis.livraison}}</p>
        <p><span class="devisp">Lieux :</span>{{devis.lieu}}</p>
        <p><span class="devisp">Commande :</span><br>
        {{devis.commande|nl2br}}</p>
        <div class="btn3">
            <a href="{{ path("admin-devis-edit", {id:devis.id}) }}" type="button" class="btn btn-primary">Modifier</a>
            <a href="{{ path("admin-devis-delete", {id:devis.id}) }}" type="button" class="btn btn-danger">Supprimer</a>
        </div>
    </div>
    {% endfor %}
</section>

{% endblock %}